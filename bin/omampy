#!/usr/bin/env python3
"""Launcher for the omampy CLI, runnable straight from a plugin checkout."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.realpath(__file__))))

from omampy.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
